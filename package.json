{
  "name": "quick-test",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=18.13.0",
    "pnpm": ">=7.25.0"
  },
  "packageManager": "pnpm@7.25.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
